Sur la page upload, seul le type MIME est verifie, il faut donc modifier la requete POST pour faire croire qu'on envoit du jpg alors qu'on peut envoyer n'importe quel fichier sur le serveur.

On peut modifier la requete POST avec firefox et l'inspecteur d'element (Firefox > Reseau > Requete POST uplaod > Modifier et envoyer ? Content-Type = image/jpg au lieu de par exemple text/php et sa nous renvoie le flag en reponse)
